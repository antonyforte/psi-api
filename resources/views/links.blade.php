
    <div>
        <h1>Links dos Ir e Is</h1>

        <p>Link Ir: <a href="{{ route('ir.show', ['session_id' => $ir->session_id]) }}">{{ route('ir.show', ['session_id' => $ir->session_id]) }}</a></p>
        <p>Link Is: <a href="{{ route('is.show', ['session_id' => $is->session_id]) }}">{{ route('is.show', ['session_id' => $is->session_id]) }}</a></p>
    </div>

